# ── Input files ──────────────────────────────────────────────
database_format: "silva_notax"           # Options: "silva", "silva_notax", or "bold"
input_primary: "00_raw/DADA2_EUK_SSU_v2.0.fasta"
input_secondary: ""                # Optional: leave empty ("") to skip
target_classes_file: "config/target_classes.txt"

# ── Outgroup sampling ───────────────────────────────────────
n_outgroups_total: 200             # Total outgroups to sample
random_seed: 42

# ── Taxonomy resolution ─────────────────────────────────────
# Path to cross-project master taxonomy list (leave empty "" to disable)
master_taxonomy_path: "/Volumes/Samsung_1TB/Zooplankton/git_DBs/Github_config/testing/SILVA_scripts/03_taxonomy_fixed/master_taxonomy.csv"
sleep_between_batches: 5

# ── Clustering ───────────────────────────────────────────────
vsearch_identity: 1.0
vsearch_threads: 8